# local env / secrets
.env
*.db
*.sqlite
*.sqlite3

# python
.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
build/
dist/
.coverage
htmlcov/

# data / artifacts
data/
*.csv.gz

# frontend
frontend/node_modules/
*.tsbuildinfo
# frontend/dist/ is intentionally COMMITTED (re-included below) so the deck runs with
# Python only — no Node needed. Rebuild + recommit it after any UI change.
!frontend/dist/
!frontend/dist/**

# generated setup charts
charts/
